# Runtime dependencies (kept in sync with pyproject.toml [project.dependencies]).
# Used by the Docker image for a simple, reproducible pip install.
Django>=5.0,<5.2
psycopg[binary]>=3.1
django-environ>=0.11
django-htmx>=1.17
whitenoise>=6.6
gunicorn>=22.0
celery>=5.3
redis>=5.0
